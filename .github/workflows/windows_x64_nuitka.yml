name: Build - Windows x64 (nuitka)

on:
  workflow_dispatch:
    inputs:
      artemis_version:
        description: 'Version'
        required: true
        default: '4.1.0'
        type: string

jobs:
  build-windows-x86_64:
    runs-on: windows-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v6

      - name: Install the latest version of uv
        uses: astral-sh/setup-uv@v8.2.0

      - name: Install dependencies
        run: uv sync

      - name: Build and package
        run: ./scripts/Windows/build_nuitka.ps1
        shell: powershell

      - name: Building installer (ISCC)
        run: iscc ./scripts/Windows/windows_installer.iss

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: Artemis-Windows-x86_64-${{github.event.inputs.artemis_version}}
          path: ./scripts/artemis.exe
