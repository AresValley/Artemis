name: Build - macOS (nuitka)

on:
  workflow_dispatch:
    inputs:
      artemis_version:
        description: 'Version'
        required: true
        default: '4.1.0'
        type: string

jobs:
  build-macos-x86_64:
    runs-on: macos-15
    steps:
      - name: Check out code
        uses: actions/checkout@v6

      - name: Install the latest version of uv
        uses: astral-sh/setup-uv@v8.2.0

      - name: Install dependencies
        run: uv sync

      - name: Build and package
        run: sh ./scripts/macOS/build_macos.sh
        shell: bash

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: Artemis-macOS-x86_64-${{github.event.inputs.artemis_version}}
          path: ./app.dist/
